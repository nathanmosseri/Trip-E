import React from 'react'

export default function Header() {

  // pass in props the current user's name and use it in the welcome user.name?
  return (
    <div>Welcome back, __________!</div>
  )
}
